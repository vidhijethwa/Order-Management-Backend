# Installation

You need to write the following commands on the terminal screen so that you can run the project locally.

```sh
1. npm install
2. npm run start
```

# Set Env
PORT=8002
USER_NAME=example
PASSWORD=example@123
MONGO_URL=mongodb://localhost:27017/orderMangementDb